class ApplicationController < ActionController::Base
  # before_action :authenticate_user!
  # Disable the web-based CSRF protection
  # protect_from_forgery with: :null_session
  #(testar depois esta proteçao)
end
